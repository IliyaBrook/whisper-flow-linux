{
  "name": "wispr-flow-linux",
  "version": "1.0.0",
  "description": "Wispr Flow for Linux - repackaged from Windows Electron build",
  "scripts": {
    "download": "node scripts/download.js",
    "extract": "node scripts/extract.js",
    "patch": "node scripts/patch.js",
    "rebuild-native": "node scripts/rebuild-native.js",
    "package-deb": "node scripts/package-deb.js",
    "package-appimage": "node scripts/package-appimage.js",
    "build": "yarn run download && yarn run extract && yarn run patch && yarn run rebuild-native && yarn run package-deb",
    "build:appimage": "yarn run download && yarn run extract && yarn run patch && yarn run rebuild-native && yarn run package-appimage",
    "clean": "rm -rf build dist tmp",
    "test": "jest --verbose",
    "run-app": "./dist/Wispr_Flow-1.4.484-x86_64.AppImage"
  },
  "dependencies": {
    "asar": "^3.2.0",
    "electron-packager": "^17.1.2"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "jest": "^30.2.0"
  }
}
